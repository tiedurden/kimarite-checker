# Install torch FIRST, from the CUDA index (see README) -- pip's default PyPI
# torch is CPU-only and will silently ignore your GPU.
transformers>=4.46
scikit-learn>=1.5
numpy>=1.26
joblib>=1.4
yt-dlp>=2024.8.6
